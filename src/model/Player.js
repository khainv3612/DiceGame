export default class Player {
  constructor(id, name, score, rollScore, isWin) {
    this.id = id;
    this.name = name;
    this.score = score;
    this.rollScore = rollScore;
    this.isWin = isWin
  }
}
